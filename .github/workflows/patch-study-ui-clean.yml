name: Patch clean study UI once

on:
  push:
    branches: [codex/study-ui-clean]
    paths:
      - .github/workflows/patch-study-ui-clean.yml
      - .github/scripts/patch_study_ui_clean.py

permissions:
  contents: write

jobs:
  patch-and-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: codex/study-ui-clean
          fetch-depth: 1
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
      - name: Install Tauri system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libxdo-dev
      - name: Import previously verified study UI files
        run: |
          git fetch --depth=1 origin codex/study-ui
          git checkout FETCH_HEAD -- desktop/src/StudyPanel.tsx desktop/src/studyApi.ts
      - name: Integrate study UI with current desktop
        run: python .github/scripts/patch_study_ui_clean.py
      - name: Verify frontend
        working-directory: desktop
        run: |
          npm ci --no-audit --no-fund
          npm run lint
          npm test
          npm run build
      - name: Verify Rust shell
        working-directory: desktop/src-tauri
        run: |
          cargo fmt --all -- --check
          cargo check --locked
          cargo clippy --locked --all-targets -- -D warnings
          cargo test --locked
      - name: Commit verified port and remove one-time patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/scripts/patch_study_ui_clean.py .github/workflows/patch-study-ui-clean.yml
          git add desktop/src/StudyPanel.tsx desktop/src/studyApi.ts desktop/src/App.tsx desktop/src-tauri/src/lib.rs
          git commit -m "feat: add middle and high study desktop UI"
          git push origin HEAD:codex/study-ui-clean
